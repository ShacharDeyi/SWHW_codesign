[project]
name = "pyperformance_bm_json_dumps_opt1"
requires-python = ">=3.8"
dependencies = ["pyperf"]
urls = {repository = "https://github.com/python/pyperformance"}
dynamic = ["version"]

[tool.pyperformance]
name = "json_dumps_opt1"
tags = "serialize"
